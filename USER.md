User goes by "jarjar" (Discord: jarjarpfeil). Building modular ECS game engine in C# on Windows natively. .NET 10, Vulkan SDK 1.4.350.0, AMD RX 6950 XT GPU, 4K display @ 150% DPI. Project: D:\Modulus-Game-Engine\. Prefers working code over verbose explanations. I run in git-bash (MSYS2/MINGW64) on Windows — can build, headless-test, AND launch the GUI editor directly.
§
User prefers that I read log files directly from disk (D:\Project-Modulus-Game-Engine\editor-*.log, pipeline-debug.log) rather than asking them to copy-paste. User wants the engine to be easily testable by AI agents via the command server to minimize need for user interaction during testing. User's display is 4K at 150% DPI scaling — all Win32 child window operations must use DPI-scaled physical pixels (multiply by TopLevel.RenderScaling). User expects me to build self-contained debugging infrastructure (headless mode, API endpoints, debug overlays, automated test scripts) rather than relying on manual GUI testing. When I make changes, I should verify them via the API or headless mode, not ask the user to test each change manually.
§
User wants me to launch the GUI editor myself (from git-bash) and use the HTTP API + screenshot + vision_analyze to debug graphical issues. Don't ask the user to test each change. The 10-iteration blind-recompile loop is an anti-pattern — tune live via API/settings dialog, then bake constants.
§
User (jarjarpfeil) is extremely patient and thorough — continues testing through many failed fix iterations without giving up. Relies on other chatbots like claude or gemini for detailed image and file analysis and shares its suggestions as message.txt files. Has decent game development GPU pipeline knowledge, will sometimes ask for explainations. Frustrated by: (1) me making analytical changes that don't fix the root cause, (2) the round-trip of "fix → test → still broken → guess again". Wants targeted, verifiable fixes where the root cause is clear before applying a change. Prefers me to read logs and analyze data directly rather than asking them to manually describe results.
§
User is technically sharp — knows how perspective works, wants precise incremental fixes. Gets frustrated when multiple changes are made at once that make things worse. Prefers small, targeted changes that can be tested individually rather than large sweeping refactors. Responds well to understanding the root cause rather than masking symptoms.
§
Design philosophy: Mods must connect automatically without requiring game code changes or rebuilds. The engine handles mod discovery/loading — game developers write `new Game(); game.Run();` and mods just work. Also: "any system that we have actively changed needs to be rebranded, if its still the same package we leave it alone" — pragmatic about renaming scope.